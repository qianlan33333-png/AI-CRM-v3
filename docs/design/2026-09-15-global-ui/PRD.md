# 新CRM 全局 UI、标准组件与经营首页优化

日期：2026-09-15
状态：已批准，进入实施；本次治理更新继续由 PR #297 交付
当前实施基线：`main=0e73d42c433892a1f5088eea08d3a079c1c98455`
实施方式：按可观察能力拆分独立工作树与 PR；本文不把任一未合入分支、CI 通过或本地截图视为已发布能力。

本次文档核对以 GitHub 当前 main 和各 PR 的实际 head 为准。独立工作树为 `/private/tmp/aicrm-global-ui-governance-20260915`；原目录只读保留，冻结 donor 不修改。本 PR 只补治理入口、产品要求、组件调用清单和验收状态，不改业务源码、不合并、不部署。

## 开发前分类

```text
OneID: reads canonical customer；仅客户相关页面读取 canonical customer 和既有稳定 Read Port，本次治理不新增客户主键、身份匹配、隐式建客或合并。
Persistence: overview 及本次视觉/组件工作为 stateless/read-only；不新增汇总表、内部任务或本地事实缓存，已有写操作继续由所属领域在既有 PostgreSQL Unit of Work 内完成。
External Effects: not involved in overview 与统一视觉本身；不新增 Provider write。发送、支付、分账、群发和调度继续由所属业务模块及 outbound/External Effects 合同负责，UI 不把 accepted/queued 当作 Provider 成功。
```

## 1. 目标与交付范围

统一所有后台一级、二级页面、导航、弹窗和抽屉，以及企微侧边栏、商品页、支付页、问卷页、分销主页。允许优化信息结构与操作入口，保留业务规则、权限和数据。

标准组件作为独立核心交付，先于业务页面改造完成。当前 main 已通过 PR #317 合入 V3 共享选择会话、标签、客服、群聊、素材、话术／内容展示和状态示例的代码与测试；这只证明对应源码和 CI 证据存在，不代表所有实际调用页完成验收。真实调用与缺口以 `skills/aicrm-v3-frontend-consistency/references/component-map.md` 为准，冻结 donor 保持不变。

使用已核实最新主线，在独立工作树开发，保留当前目录修改。已退出计划模式，现授权实施。

## 2. 图像模板与视觉规范

已验证个人 Product Design 图像模板「新CRM · 小鹅通式统一工作台」，目录为 `/Users/qianlan/.codex/skills/artifact-template-crm`，模板显示名以 `artifact-template.json` 为准。第 3 张 PNG 为主参考；前两张约束用户档案和详情页，第 4 张约束经营指标区。reference／preview 的 SHA-256 为 `84f24c0c9d43b624db1a652599d27d42b18b9b046c74cdac4ff77f059b4bfce4`。

本轮不重做模板、不重新索要图片、不把模板存在当作页面验收。模板只约束视觉方向，代码继续通过 v3 共享组件落地：统一浅灰背景、白色内容区、蓝色主操作、字体层级、间距、圆角与状态色；后台、企微侧边栏和用户手机页共享视觉语言，同时保留各自布局和认证边界。含用户数据的截图不提交 Git，模板文件与原图仍属于个人技能目录。

## 3. 标准组件专项

| 组件 | 统一能力与边界 |
|---|---|
| 标签选择器 | 分组、搜索、单选/多选、已选回显、移除和数量提示；区分标签来源与可选范围。确认选择不自动给客户打标。 |
| 群聊选择器 | 搜索、分页、多选、已选回显与不可选原因；真实群聊使用领域提供的 `chat_reference`，与群邀请素材分开，不能混用标识。 |
| 客服/员工选择器 | 搜索、单选/多选、已选回显和目录刷新；按调用页面授权范围读取。刷新失败保留原列表与选择，不默认套用群运营范围。 |
| 话术＋素材编辑器 | 文本编辑、允许的变量插入、素材添加与排序、删除、预览、校验和只读展示；固定话术与 Prompt 保持清楚区分，按业务场景提供能力。 |
| 素材选择器 | 统一图片、附件、小程序等类型的浏览、筛选、预览、单选/多选与回显；展示失效及不可用原因。上传入口由调用场景和权限决定。 |
| 内容预览与只读详情 | 编辑预览和保存后详情复用同一套内容呈现规则，保持文字、变量、素材顺序一致；明确预览、保存和实际发送结果的区别。 |
| 通用基础组件 | 统一筛选栏、日期范围、表格、分页、批量操作、状态标签、详情抽屉、确认框、Toast，以及加载、空态、失败和无权限状态。 |

所有选择器共同遵循：调用方明确数据来源、授权范围、选中值、单/多选、数量限制及只读状态；服务端继续执行权限校验。弹窗内先维护临时选择，确认后才回传；取消不改原值，重复打开正确回显。搜索结果乱序返回时不覆盖新结果；分页不丢选择，失败不清空已有内容。已失效或失去权限的选中项明确提示，不静默删除或替换。支持键盘操作、焦点回收和窄屏适配；不同实体标识不能互相替代。

话术与素材组件只负责编辑、校验和展示。保存、上传、发送、调度及 Provider 调用继续由所属业务模块承担；预览不能触发真实发送。

组件统一放在 v3 拥有的共享层，扩展现有 Host/Adapter，保持冻结 donor 不变。建立组件目录、调用页面清单和状态示例页，业务页面不得再维护平行实现。公共用户页面不挂载后台客服、群聊等内部选择能力。

## 4. 经营首页与全页面改造

首页保留 `/admin`，按统计范围→核心指标→经营趋势→分销概览→待办异常与入口组织。核心指标为确认支付金额、支付订单数、支付客户数、新增客户数；补充退款金额和净收款。分销区分期间业绩、待结算和已结算金额。

新增只读 `GET /api/admin/overview`，返回统计区间、指标、趋势、更新时间及分区数据状态。overview 经各领域稳定 Read Port 获取授权范围内的汇总，webshell 只展示，不跨领域查表、不触发 Provider、不新增持久化汇总表。

统计采用北京时间，金额按最小货币单位计算并保留币种；支付和退款分别按确认时间计算。客户使用 canonical customer 去重，迁入时间不冒充新增时间。无权限、不可用、未知和真实零值分别呈现，指标下钻保持相同统计口径。

后台导航调整为总览、客户、运营、交易、分销、内容素材、系统设置，保留旧链接兼容与既有权限，安全配置继续仅向超管开放。

重点模块：分销管理统一指标/筛选/表格/详情抽屉/异常处理反馈，突出佣金状态与订单证据；运营闭环统一客服群聊选择、话术素材编辑和只读详情，清楚呈现计划批次准备执行回执，保留标题封面发送约束；用户档案统一身份摘要、标签选择、业务记录及详情展示，保持 OneID 客户归属规则；素材库库内浏览与业务页选择共用规则；自动化话术复用话术素材编辑、变量插入、预览，保留 Prompt 配置。

商品编辑页保留一个分销启用开关，分销配置只在“售卖信息”显示；开关下方仅配置本商品分销比例与退款复核等待天数。页面素材、购买后动作、企微标签、外部推送等原有业务板块继续保留，但其中不再显示分销配置；商品编辑页移除分销员申请入口、申请链接、复制按钮和二维码，公共分销中心不变。保存其他维度不能覆盖分销值，切换 tab 保留草稿。

### 交易订单中的分销事实

交易列表在授权范围内展示归因分销员与佣金状态摘要；订单详情展示该订单成交时冻结的佣金比例、归因分销员、佣金金额和币种、退款复核等待期、预计结算时间，以及实际结算／分账时间、状态与异常证据。预计时间与实际时间必须分列呈现，不能以预计时间冒充已分钱，也不能用商品当前比例替代订单成交时快照。

未参与分销、数据未知、无权限和真实零佣金分别展示。列表摘要不得据此推断订单归因；详情中的任一字段缺失均按该字段的可用状态呈现，不补造比例、人员、时间或状态。

订单页面只经 distribution 的稳定 Read Port 读取订单级分销事实，并保留交易领域的既有订单读取边界；不得跨领域查表、写入佣金记录或触发分账。若现有 Read Port 尚未提供订单快照、复核期、预计／实际结算时间、状态或证据，先由分销领域扩展版本化只读合同并完成真实数据验收，再挂载 UI。

### 用户补充

全局文本搜索使用 Enter 或明确的搜索按钮提交。渠道码中心必须修复逐键搜索打断中文输入：`input`／composition 期间不搜索、不重绘，确认中文候选的 Enter 不能触发搜索，也不能拦截浏览器对候选词的默认确认。普通 Enter 才提交当前 draft；空词 Enter 回到调用方授权的无关键词目录，其中渠道本地筛选可使用当前页面已加载的全量结果，远程选择器必须重新请求无关键词分页目录；清空／重置同步清除已提交条件。翻页与刷新始终使用已提交条件，旧查询响应不能覆盖新查询结果，取消选择器不改原值。

搜索第一批只覆盖组件清单中登记的精确搜索控件。输入法 composition 期间不搜索、不重绘；候选确认的 Enter 保留给输入法，普通 Enter 或明确搜索按钮才提交当前 draft。实现和实际宿主范围以执行状态为准；其他页面必须按真实 Host、查询边界和错误语义逐页接入，不能因共享模块存在而视为全局完成。每个接入项都覆盖真实 composition、候选确认、普通 Enter、清空、刷新、分页与异步乱序。

其余后台页面全部纳入同一清单。企微侧边栏采用窄屏组件适配并保留上下文授权；商品、支付、问卷及分销主页分别优化主要操作与成功、失败、恢复状态。

## 5. 实施顺序与验收

1. 页面组件盘点、业务判断、GitHub 参考、综合 PRD，以及主线缺失的前端一致性 Skill 入口补齐。
2. 创建图像模板统一视觉变量，完成标准组件与状态示例页。
3. 完成后台导航、经营首页和只读汇总。
4. 逐项完成 5 个重点模块并验证组件真实接入。
5. 完成其余后台、企微 sidebar 及全部用户展示页。

root 负责分发审核，Terra high/xhigh 开发，Luna max 基础执行审计。每个 PR 交付一个可观察能力。

验收：所有实际调用页覆盖搜索、回显、确认、取消、刷新、失败、权限变化和多选；编辑、预览、保存、详情一致，重复点击不重复业务操作；订单分销信息须以权威订单快照与真实分钱事实回读，分别覆盖未分销、未知、无权限、退款复核、预计结算、已结算和异常证据；所有页面状态逐项通过，后台 1280/1440、sidebar 360/420、用户 375/390/430 视口均验证；首页与明细一致，退款、迁入、跨午夜、权限差异和单领域故障均有验证；支付、问卷提交、分销归因、运营执行合同不退化；运行受影响的 typecheck、build、shell、Journey，并与同视口参考对照；发布后分别进行版本、页面回读和业务验收。

## 6. 当前证据与验收级别

页面台账把 C0–C3 当作证据级别，而非完成状态：C0 是 canonical 路由、Host、manifest assets 的静态核对；C1 是共享组件合同测试；C2 是实际挂载壳和视口证据；C3 是认证业务数据、关键交互以及保存后服务端 readback。每一页只按明确证据取最高级别；CI 绿灯、组件存在、注入 Host、截图或 fixture 成功都不能自动升级整页。99 条台账包含 route、alias、reserved placeholder、login/logout 和构建 artifact，不等于 99 个 canonical 页面。

当前 main 已合入 #317（#287、#296、#300、#303、#305、#307、#310、#311、#315 的能力随其合入），随后合入 #324 Radar 管理列表筛选分页（merge commit `0e73d42c433892a1f5088eea08d3a079c1c98455`，head `cedc7cb98afe920c91378319361ef44b44f5bf5b`）。#317 的 run `34924244166` 与 #324 的 run `34926594842` 均 deploy skipped；本次同步前 #297 的 strict gate 为 BEHIND，现已以最新 main 为基线。线上 IAB 只读访问超时且无认证读回，不能称为已部署或生产验收。其余本任务 PR 仍按各自状态维护。Tag、Staff、Composer 的状态示例页仍缺，现有 `/admin/component-states` 只覆盖 Group、Material 和表单／IME 的本地示例；后台其余页面、企微 sidebar、H5 与公开问卷页继续逐页验收。

最终交付：图像模板引用、全局 PRD、标准组件真实调用目录与状态示例记录、全部改造页面、逐页验收记录和独立 PR 证据。本轮 PR #297 仅交付治理文档与规则入口，不能缩减已批准的全局页面改造范围。

## GitHub 参考

- [Ant Design DESIGN.md](https://github.com/ant-design/ant-design/blob/master/DESIGN.md)
- [Ant Design Pro](https://github.com/ant-design/ant-design-pro)
- [Vant](https://github.com/youzan/vant)

以上仅作信息架构、token、组件治理和移动端适配参考，不引入依赖、不复制代码、不替代 v3 共享组件和领域合同。

如有四张 PNG 视觉参考，仅保存在本工作树 `docs/design/2026-09-15-global-ui/references/` 供视觉对照，含用户数据的截图不提交 Git；个人 Product Design 图像模板由 Template Creator 写入个人技能目录，不属于仓库 commit。
