# 群邀请计划与共享群目录

实施依据：本任务中用户确认的 PRD。固定单群和按顺序轮替多群共用稳定 H5 入口；多群阈值必填且为 1–200 的整数，无默认值；不回流、不自动建群。全量扫描首次立即执行、固定每 24 小时一次，并提供人工即时刷新；计划当前及待用群每 5 分钟定向刷新。前端目录与选择器始终展示群昵称及群 ID。

## 核心分类

- OneID：不新增身份匹配、建客、归属变更；只读取群标识与人数。
- Persistence：PostgreSQL 业务配置/审计同事务；持久任务复用 jobqueue/River；Provider 读取在事务外。
- External Effects：官方单群入群码生成走 outbound 与 EER；未知结果不盲重试。
- 群目录唯一 Owner 保留 groupops，media 通过 Port 读取；不复制目录、不跨领域读写表。

## 业务验收

阈值 1/200 边界、顺序推进无回流、满池停止补群恢复；全量分页、24 小时固定日程、人工扫描合并补跑、部分失败保留记录；定向读取去重与旧结果不得覆盖新结果；真实昵称、重名 ID、未命名及待同步状态；所有既有素材引用稳定。

## 前端复用

管理端采用 admin_base 单壳与现有表格/分页/表单；公共页独立。已读取项目 frontend-consistency 与 product-design:index；用户随后明确指定 Product Design 与 artifact-template-crm；已读取参考图并按其浅灰背景、白面板、蓝色下划线页签、独立筛选表格调整，保留现有后台壳。GitHub/市场参考已在本任务调研：EasyInk 群码选择逻辑、微伴自动拉群；不复用扫码量为人数的实现。

## 交付门槛

当前工作区存在用户并行改动，保留原样。实现、局部测试、完整 CI、真实企微生成权限与实际扫码入群分别报告。真实验证仍需要测试群与测试微信；不将本地实现称为生产验收。


## 临时兼容适配

Owner: media / webshell，Composition 挂载共享 group_chat_picker.js 的 V3 邀请计划实现。替代路径为稳定的群邀请计划 API；各消费者迁离冻结供体后删除该路径兼容映射。旧素材 ID 保留，运营在邀请计划页面显式绑定升级。

## 尚需真实环境验收

测试群名：测试群，用户可提供测试微信；尚未生成真实官方群码或扫码。完整 CI 未执行，不以当前本地测试代替。
