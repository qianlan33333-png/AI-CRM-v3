# PR #164 新前端壳接入与上线 PRD

状态：2026-09-06 用户明确要求在现有开发成果上把 PR #164 一同上线。该 PR 保持独立，五项业务仍各自一个完整 PR；不恢复大型总集成 PR。根负责需求、审核与发布协调，执行智能体实现。

## 来源和范围

冻结待接入来源：AI-CRM-v3 PR #164，原分支 feature/new-shell-swap，准确 c8819d7a9b837c00116682e860310717e5f94b20。保留其本地新壳、深链接规范化、侧边栏真实协议适配、登录页和缩略图实现，修复当前失败并接上最新 main 及五板块最终接口；不重新设计页面或业务后端。

旧行为供体仍为 AI-CRM dd8d60dd8ddb983aca2ec88cc9e65a9f7563f79f，已有V2冻结前端和各模块Host继续作为完整交互依据。最新用户要求采用PR164新壳优先于早期壳层选择，但不是删除现有完整业务流程的授权。

## 分类与依赖

OneID：侧边栏只读取上下文认证后的canonical customer；手机号绑定等写入沿用当前Identity Port可信流程，不把请求手机号或前端字段自报为verified。持久化：本板块主要无状态Host与HTTP适配，业务写继续使用既有领域服务/UoW、receipt与幂等。素材发送仍由已批准send-intent及企微客户端结果链控制，不新增服务端企微写、队列或重试。

后端共同装配由执行者在PR164分支对齐最新main完成；不把未审查的171/172/173实现复制进164。可以准备兼容代码，最终在各业务PR合并后校验准确最终组合。

## 已有／待补齐

- 已有：39页本地dist读取、深层别名303、侧边栏JSSDK/OAuth与画像/手机号/素材/订单/问卷等真实DTO适配。
- 待修：当前CI34022478585在check-pr01-donor-manifest失败，新文件web/src/sidebar/tabs/local-contract.ts不属于冻结集，之后其他修改文件的hash亦需逐项核对。保留冻结供体与其完整性校验，V3适配逻辑放V3-owned Host/Adapter；复用现有构建机制。不得更新hash把修改冒充未变，不得删门禁。必要的已明确V3-owned文件沿用现有归属，不重复搬迁。
- 待接：当前已合并通用客户标签169、运行配置170，以及负责人171、外推172、开放平台173的页面、动作、结果与权限；新壳不能遮住这些真实Host，也不能用原壳占位/HTTP200代替完整流程。
- 待验证：资产manifest/缓存/深链query保留、登录与注销、会话/CSRF、侧边栏无后台session时静态资源可读但客户API仍需上下文授权、图片缩略图权限/ETag、分页与标签过滤。

## 用户流程与兼容

1. 管理员登录后进入新壳，导航及一二级页面链接可达，刷新/直达/返回保持当前对象和查询条件；未登录受保护页面必须走实际认证。
2. 客户列表保留标签批量选定/确认/结果回读；负责人迁移保留员工选择、范围/Excel、预览、确认、实际结果及导出；配置保留发布/实际有效值/回滚；商品外推保留配置与测试投递及订单结果读取；开放平台保留调用方管理/凭据生命周期与旧接口目录。
3. 必要时在新壳下复用已有业务Host呈现正文；不要用壳层切换撤掉已验收业务能力。统一导航布局，不新开发业务规则。
4. 侧边栏使用真实Provider上下文与当前OneID；图片/素材选择、发送授权和结果状态保持原链路，未知结果不伪报成功。没有真实Provider证据的现场场景单独列明。
5. 公共购买、OAuth回调、会话存档、OneID与人群等原可用入口继续按其真实Owner路由工作；不得因dist广泛匹配抢占API、OAuth或公开购买路径。

## 测试与交付

先复现具体门禁和构建失败，再通过相关Go/TS测试、冻结来源检查、build、实际HTTP装配，最后全CI。测试不得仅用mock响应模拟完成。

真实PostgreSQL+Chromium至少覆盖登录与深链、新壳关键业务入口、标签写入回读、配置发布生效、迁移预览确认结果、商品外推保存/测试/订单视图、开放平台创建停用/密钥轮换及授权读取。复用各板块专项，在最终164组合上跑；不要重写或弱化原断言。侧边栏协议测试覆盖上下文授权失败/成功、CSRF、素材send-intent及结果未知。实际企微webview验收需要Provider上下文，和测试Provider证据区分。

PR包含必要后端适配、最小前端修改、构建制品与部署契约、测试证据和最终准确HEAD。若无数据库模式或历史迁移变化，明确不涉及，禁止为了清单制造迁移。

## 上线条件

用户本次明确授权完成开发后上线，替代之前仅开发/PR边界。根审核准确HEAD、CI与最终组合关键流程后按既有流程合并发布；核验生产readyz准确SHA、新壳真实资源和主要只读页面，执行已授权必要配置接入。密钥仍只在受保护文件，不输出到对话/PR。真实扣款、退款、批量企微发送或批量交接不由“上线”自动推导，业务验收须使用明确测试对象/现有授权。

部署成功、配置生效、历史导入、真实Provider验收分别记录；上线不能只依据main合并或HTTP200。回退采用既有发布版本机制，数据库迁移保持向前兼容。旧系统继续独立存在，不停机、不切流。

## 根预审发现（准确c8819d7）

- 现有release仍走stage-pr01-effects-ui.mjs有限页面/资源白名单，不能保证164需要的39页与sidebar/index及动态chunk进入制品。需要在既有发布工具中登记新壳入口和递归依赖闭包，对真实stage产物执行浏览器验收。只在开发web/dist显示通过不足以上线。
- 新侧边栏使用blob URL显示缩略图，当前CSP只允许Media页面的img-src blob；需要限于侧边栏页面的兼容与实际naturalWidth验证，保持其他页面限制。
- 新的缩略图API须复用Media可见性判定，检验停用/归档图片不通过猜测ID绕过侧边栏列表约束。
- 当前AI/Channel/Product/GroupOps挂载删除了既有UI binding，而真实业务Host仍承担字段适配和执行按钮。新壳必须接回同一业务链后才可替换；不能仅以新页面成功渲染为准。

## 开放平台最新范围覆盖

用户已明确采用03-open-platform.md与ADR0010的六个原生V1 Operation，旧56接口目录和等价迁移停止。PR173继续同一完整板块PR，REST /open/v1与MCP /mcp共享处理器/权限/OneID/幂等/审计。PR164只接调用方列表详情、创建及一次secret、轮换停用、grant、审计和V1 capability catalog，不显示旧56目录。负责人、标签、配置、商品外推及其他新壳交付要求保持现行已授权范围。
